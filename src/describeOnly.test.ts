describe.condition(true).only('describe condition skip', () => {
  it('describe condition only', async () => {
  });
});

describe('describe condition only should skip', () => {
  it('describe condition only should skip', async () => {
  });
});
